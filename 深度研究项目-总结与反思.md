# 深度研究项目 · 总结与反思

> 合并自：D2笔记×2 / 改造审查报告 / 4项改造 / checklist升级 / 聚合流程设计 / 知识库方案要点
> 精简日期：2026-08-11 ｜ 企业知识库管理助手（深度研究项目）

---

## 一、学习成果总结（零基础 → 能讲清项目）

**已掌握的知识清单**（D1-D3 全程）：

| 类别 | 具体内容 |
|------|----------|
| Python 基础 | 变量/函数/类/import、dataclass（kw_only/field）、枚举、类型注解、字典展开 `**`、f-string、三元表达式 |
| 进阶概念 | 生成器 yield、装饰器、闭包、Thread + Queue + Lock、Semaphore、`for _ in gen: pass`、nonlocal、@staticmethod、Annotated[list, operator.add]（reducer） |
| 后端框架 | FastAPI 路由/请求模型/StreamingResponse、SSE 协议（data:{json}\n\n 分隔）、Pydantic vs dataclass（信任边界） |
| Agent 架构 | 多 Agent 协作（规划/总结/报告）、工厂模式防串话、Agent 无状态、提示词工程（XML 分区/few-shot/兜底指令/模板控结构） |
| 前端 | Vue3 ref/reactive/computed、模板指令、SSE 客户端手写（fetch + ReadableStream）、打字机效果 |
| 工程 | 配置化铁律、多级降级链、KV+TTL+LRU 缓存、请求级限流、重试退避 |

**学习模式**：零基础逐行精读 + 大白话类比 + 面试官视角纠错 + 每日检验题（被纠正过多次：锁保护的是写入过程非答案内容、Semaphore 压的是峰值非总量、SWR 省的是等待非搜索次数、`|None`是类型声明非判断）。

---

## 二、改造成果（4 项 + 1 升级，全部验证）

| # | 改造 | 验证证据 |
|---|------|----------|
| 1 | Semaphore 限流 + 配置化（MAX_CONCURRENT_WORKERS） | 实测最多 N 个并行；改 .env 即调 |
| 2 | 搜索缓存（KV+TTL+LRU+命中率日志+空结果不缓存） | 2.24s → 0.00s，过期重建，LRU 淘汰正确 |
| 3 | 任务级重新生成（regenerate 接口+前端按钮） | 只重跑目标任务，新总结替换进报告 |
| 4 | 多轮研究（checklist 缺口评估+收敛控制） | 缺口触发第 2/3 轮、无缺口提前收敛、总结聚合 |
| 升级 | checklist 结构化评估（拆解→逐项核对） | 对空泛声称不买账，稳定触发多轮 |

**附加**：DuckDuckGo → Tavily 切换（可插拔搜索源）、.gitignore 安全补漏、DeepSeek/Tavily key 轮换。

---

## 三、审查发现与修复（20 问题）

**P0 正确性（已修）**：
1. 评估解析失败默认收敛 = 静默丢缺口 → 改为保守补搜
2. 每轮重复拆 checklist → 任务级懒加载复用（拆 1 次）
3. 评估只看本轮总结 → 累计总结
4. 空结果被缓存 600 秒（缓存毒化）→ 空结果不缓存
5. （顺手）NO_GAP 子串误判 → startswith 精确匹配

**P1 短期（已修）**：研究深度选择（1/2/3 轮）、请求级搜索限流、regenerate 笔记同步、缓存 LRU、命中率日志
**P2 体验（已修）**：排队提示、按钮进行中文案、轮次分节、gap 查询词增强、checklist 限长、TTL 关键词分档
**已决策不做**：会话缓存（单机收益有限，保留无状态设计，生产再引入）

---

## 四、设计资产（可复用的方法论）

1. **三层聚合流水线**（《多Agent答案聚合流程设计》）：摘要层（并行独立）→ 对比层（去重/共识/分歧）→ 主编层（四层模板报告）。已被 AI 内容工厂吸收。
2. **RAG 演进方案要点**（《企业级知识库问答系统演进方案》）：双链路架构（入库/问答）+ 权限两级模型 + RAGAS 评估 + 反馈闭环；技术选型表（Qdrant/BGE-M3/自研编排）仍可用。
3. **工程化改造蓝图**：手写编排 + 吸收 LangGraph 思想（checkpoint/HITL/time-travel）自研实现；测试 mock LLM 层；loguru + request_id。

---

## 五、反思与教训

1. **先盘点再动手**：新项目立项先做资产盘点（内容工厂 90% 复用现有架构），不重复造轮子。
2. **LLM 输出不可信**：所有解析都要兜底链（剥 think → JSON → 正则 → 默认值），所有 API 调用都要降级链。
3. **设计 ≠ 实现**：蓝图里的修正必须落地成代码才算数（checklist 升级、P0 修复都是先设计后验证）。
4. **画像信息需核实**：云端画像曾包含错误职业信息（已纠正），引用前先与用户确认。

---

## 六、面试弹药（核心话术）

> "深度研究助手：TODO 驱动 + 三 Agent 协作 + 多线程并发 + SSE 流式。我做了 4 项改造：Semaphore 配置化限流（压峰值非总量）、搜索缓存（KV+TTL+LRU，2.24s→0s）、任务级重新生成（增量更新）、多轮研究（checklist 结构化缺口评估 + 收敛控制，防止过早收敛漏信息）。生产级要点：多级降级链、Agent 无状态（工厂+清历史防串话）、配置化铁律、事件契约化测试。"
