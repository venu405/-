# 企业知识库管理 · 交接文档

> 版本：v2（2026-08-12 16:30，覆盖 v1；P1 + P2 已完成）｜ 项目位置：`C:\Project\企业知识库管理助手`
> 用途：新会话直接读本文件，即可完整接手项目上下文。

---

## 一、项目一句话

> 在**深度研究项目**（FastAPI + Vue3 + SSE 多 Agent 编排）基础上，用 **LangGraph + RAG** 构建的**企业知识库管理**系统：文档入库（解析→分块→向量化→Chroma）+ 智能问答（改写→混合检索→生成→忠实度评估，带引用溯源）。

**两个项目关系**（都在本项目目录下）：
- 深度研究（搜索 Agent）：`code/backend/src/agent.py` 等——手写 Thread+Queue 编排
- 知识库管理（本交接重点）：`code/backend/src/services/kb/`——LangGraph 状态图编排

> 面试叙事："同一团队两个项目，深度研究用手写编排（要掌控 SSE 事件流），知识库问答用 LangGraph（检索/生成/评估是天然图结构，条件边原生表达'评估不达标就重试'）。按场景选工具，不为用框架而用框架。"

---

## 二、技术栈

| 组件 | 选型 | 说明 |
|------|------|------|
| 编排 | **LangGraph 1.2.10** | StateGraph：rewrite→retrieve→generate→evaluate + 条件边重试 |
| 向量库 | **Chroma 1.5.9** | pip 即用；**数据目录在 `src/chroma_data/`（相对路径按后端启动目录解析！）** |
| 混合检索 | **rank_bm25** + RRF 融合 | 向量 Top-N + BM25 Top-N → RRF（k=60） |
| Embedding | **BGE-M3 本地（Ollama）** | 1024 维，中文强；`/api/embed` 批量接口 |
| 重排 | bge-reranker-v2-m3 | **暂缓**（需 torch + 2GB 模型，小语料收益有限） |
| LLM | **DeepSeek**（OpenAI 兼容客户端） | `deepseek-chat`，复用现有 key |
| 解析 | pypdf + python-docx + markdown | MD/TXT/PDF/docx |
| 后端 | FastAPI（4 个 KB 接口） | /kb/ingest /kb/ask /kb/docs /kb/docs/{id} |
| 前端 | Vue3 + SSE | KbChat.vue 对话页（已实测通过） |

---

## 三、架构：双链路

```
【入库链路 · 离线】
上传文档 → ingest.py 解析分块（chunk_size=800, overlap=100）
  → embeddings.py BGE-M3 向量化（1024维）
  → vector_store.py 写入 Chroma（metadata 带 doc_id/doc_title/chunk_index）

【问答链路 · LangGraph 实时】⭐
用户问题 → node_rewrite（多轮上下文改写）
  → node_retrieve（混合检索：向量+BM25+RRF，Top-K）
  → node_generate（拼上下文 → DeepSeek 生成答案 + [n] 引用标注）
  → node_evaluate（LLM 忠实度打分 0-10，<6 不达标；失败退回规则检查）
  → 条件边：不达标且 retries<1 → 回 generate 重试；否则 END
```

**两阶段 RAG 范式**（面试核心）：召回层（混合检索）已做，精排层（reranker）暂缓——"等语料千条级再上，接口可插拔"。

---

## 四、当前进度快照（P1 ✅ + P2 ✅）

| 功能 | 状态 | 验证证据 |
|------|------|----------|
| bge-m3 模型 | ✅ | Ollama `bge-m3`（1.2GB），1024 维，语义排序正确 |
| POST /kb/ingest | ✅ | 上传→分块→向量化→Chroma；`title` 参数生效（显示友好标题） |
| POST /kb/ask | ✅ | "超过5万元必须招投标[1][2]"带引用；无关问题诚实拒绝（防幻觉） |
| **混合检索**（P2） | ✅ | BM25 精确命中"5万元"、向量语义命中"年假/加班"，RRF 排序正确 |
| **LLM 忠实度评估**（P2） | ✅ | 日志 `RAG 忠实度评分: 10/10`，不达标自动重试（retries 计数） |
| GET /kb/docs | ✅ | 列表 + chunks 统计 |
| DELETE /kb/docs/{id} | ✅ | deleted_chunks 生效 |
| 多轮对话 | ✅ | KbAskRequest.history + rewrite 节点 |

**当前测试数据**：Chroma 有 2 份文档（采购管理制度 1 chunk + 员工考勤制度 1 chunk）。

---

## 五、环境状态与启动方法

| 服务 | 状态 | 说明 |
|------|------|------|
| 后端 :8000 | ✅ 运行中 | 启动：`cd code/backend/src && .venv/Scripts/python.exe -m uvicorn main:app --port 8000` |
| Ollama :11434 | ✅ 运行中 | bge-m3 就绪；**须 unset 代理环境变量**（见踩坑 ①） |
| 前端 :5174 | ✅ 运行中 | 启动：`cd code/frontend && node node_modules/vite/bin/vite.js --port 5174` |
| Docker | ⏸ 放弃 | 环境全正常但 Docker Desktop 4.86 创建 WSL 发行版报 HCS/ERROR_NOT_FOUND；不阻塞主线 |
| 测试套件 | ✅ 41 用例全过（KB 21 + 深度研究 20，以 `pytest --collect-only` 为准） | `code/backend/tests/` |

**接口快速验证**（curl）：
```bash
# 上传（title 可选）
curl -X POST http://127.0.0.1:8000/kb/ingest -F "file=@文档.md" -F "title=文档标题"
# 问答（history 可选，多轮）
curl -X POST http://127.0.0.1:8000/kb/ask -H "Content-Type: application/json" -d '{"question":"问题","history":[{"role":"user","content":"上一问"}]}'
# 列表 / 删除
curl http://127.0.0.1:8000/kb/docs
curl -X DELETE http://127.0.0.1:8000/kb/docs/{doc_id}
```

---

## 六、关键代码位置

| 文件 | 职责 |
|------|------|
| `src/services/kb/ingest.py` | 解析 + 分块（重叠窗口） |
| `src/services/kb/embeddings.py` | BGE-M3 客户端（Ollama /api/embed）；注释含 zhipu 备选 |
| `src/services/kb/vector_store.py` | Chroma 封装（add/search/delete/all_items/count）；where 参数是权限隔离入口 |
| `src/services/kb/retriever.py` | **P2 新增**：HybridRetriever（向量+BM25+RRF）+ VectorOnlyRetriever（对照组）；中文 2-gram 分词 |
| `src/services/kb/qa_graph.py` | LangGraph 图（4 节点+条件边）；`_llm_invoke()` 适配器；`_evaluate_faithfulness()` LLM 打分 |
| `src/main.py` | 4 个 KB 接口；`_get_kb()` 懒加载；**KbAskRequest 在模块顶层**（Pydantic 兼容） |
| `src/config.py` | `kb_*` 配置（embedding 模式/ollama host/chunk/top_k/collection） |
| `code/backend/.env` | KB_EMBEDDING_MODE=bge_m3、KB_CHROMA_DIR=./chroma_data、KB_TOP_K=5 等 |
| `code/frontend/src/KbChat.vue` | 对话页（上传+聊天+引用展示，打字机风格） |

---

## 七、踩坑记录（重要经验，防重复踩）

1. **网络/代理坑**：系统有代理环境变量 `http_proxy=127.0.0.1:7897`（代理软件未开）→ 境外域名超时。**诊断用 `curl --noproxy "*"`；服务运行前 unset 代理变量**。
2. **模型下载坑**：Ollama R2 CDN 国内反复断线。**方案：`ollama pull modelscope.cn/{org}/{model}`（Ollama 原生支持 ModelScope）**。国内模型源优先 ModelScope。
3. **Pydantic + `from __future__ import annotations` 坑**：注解字符串化导致 FastAPI 解析失败（"not fully defined"）。**修复：UploadFile 顶部 import；请求模型类定义在模块顶层**（KbAskRequest 曾定义在 create_app 内报此错）。
4. **LLM 接口适配坑**：OpenAI 客户端无 `invoke`（LangChain 风格）→ 写 `_llm_invoke()` 适配器（hasattr 分支 + chat.completions.create），客户端挂 `_model` 属性。
5. **文件上传 500**：需要 `python-multipart`（已装）。
6. **沙箱删文件坑**：`unlink` 被安全 hook 拦截 → try/except 包裹，清理失败不阻断入库。
7. **相对路径坑**：`KB_CHROMA_DIR=./chroma_data` 相对后端启动目录（src/）→ **数据在 src/chroma_data**，不是 backend/。测试脚本必须用 `Configuration.from_env()` 初始化，不能硬编码路径。
8. **评估与路由一致性**：evaluate 判定结果写入 state.low_quality，条件边读同一份——避免"一边判重试一边放行"。
9. **PowerShell 脚本编码坑**：含中文 .ps1 在 GBK 控制台乱码 → 写纯英文。

---

## 八、待办（下一步）

### ① P3 管理（未开始，建议下一步）
- 文档 CRUD 完善（更新/重新索引）、**多知识库隔离**（Chroma collection 命名空间 或 metadata kb_id + where 过滤）、增量更新、**权限**（检索层强制 metadata 过滤，绝不在生成后补救）

### ② P4 工程化（未开始）
- pytest 补 KB 模块测试（复用 tests/mocks FakeLLM 思路）、LangGraph checkpointer（SQLite 持久化对话 → 断点续跑）、评估分数前端展示

### ③ 可选增强
- bge-reranker 重排（语料千条级再上；接入点在 HybridRetriever 输出之后，接口不变）
- 文档内容更新接口（PUT 重新解析入库，先 delete_doc 再 add）

### ④ Docker（挂起，不阻塞）
- 环境层全正常（WSL2.7.11/内核 6.18.33/VMP Enabled），疑点在 Docker Desktop 4.86 兼容；留 `C:\Project\_docker_logs.ps1` 抓日志用；备份目录 `%LOCALAPPDATA%\Docker\wsl.bak_153138` 待清理

---

## 九、面试弹药速查

1. **为什么 LangGraph**："检索/生成/评估是天然图结构，条件边原生表达'评估不达标就重试'；对比深度研究手写编排——按场景选工具。"
2. **两阶段 RAG**："召回层做向量+BM25 混合检索+RRF 融合（语义与精确互补）；精排层评估过 bge-reranker，小语料暂缓，接口可插拔——知道成本收益与触发条件。"
3. **防幻觉**："evaluate 节点 LLM 忠实度打分（0-10，<6 重试），失败退回规则检查；检索不到时 prompt 要求'缺少相关信息'，实测无关问题不编造。"
4. **LLM 适配器**："OpenAI 客户端与 LangChain 风格接口不同，`_llm_invoke()` 统一，可插拔换后端。"
5. **踩过的坑=加分项**："Pydantic+future annotations、ModelScope 拉模型、代理干扰、相对路径——都是真实工程问题。"
6. **RRF 融合**："不同检索器分数尺度不同（向量 0-1 vs BM25 无界），RRF 只看排名：Σ 1/(k+rank)，k=60。"
7. **BM25 中文分词**："免装 jieba，正则切分+2-gram+停用词——够用且零依赖。"

---

*交接完毕（v2）。新会话从此文档开始，结合 `.workbuddy/memory/2026-08-12.md` 工作日志即可完整接手。*
