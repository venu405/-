# 企业知识库管理 · 完整实施计划表

> 版本：v3（2026-08-13，对照实际进度刷新；覆盖 v2）｜ 项目：基于深度研究项目 + LangGraph + RAG 的企业知识库管理助手
> 目录：`C:\Project\企业知识库管理助手` ｜ 后端：`code/backend/src` ｜ 前端：`code/frontend`
> v3 要点：P1-P4 主体已完成；Docker 路线挂起；BGE-M3 已定 Ollama；补 RBAC/增量更新/引用前端化的缺口设计；验收标准量化。

---

## 一、技术定稿（已落地，v3 刷新）

| 组件 | 选型 | 状态 |
|------|------|------|
| 编排 | **LangGraph 1.2.11** | ✅ 已装，问答图 4 节点 + 条件边重试 |
| 向量库 | **Chroma 1.5.9** | ✅ 已装，单 collection + kb_id metadata 隔离（P3） |
| Embedding | **BGE-M3 本地（Ollama）** | ✅ 已部署，1024 维，`/api/embed` |
| LLM | DeepSeek（OpenAI 兼容客户端） | ✅ 复用现有 key，`_llm_invoke` 适配器 |
| 混合检索 | rank_bm25 + RRF（k=60） | ✅ P2 完成，BM25 按 kb_id 分库缓存 |
| 文档解析 | pypdf + python-docx | ✅ MD/TXT/PDF/docx |
| 后端 | FastAPI（6 个 KB 接口） | ✅ ingest/ask/docs/kbs + PUT 更新 + DELETE |
| 前端 | Vue3 + SSE | ✅ KbChat.vue（上传/对话/评分徽标） |
| **Python 环境** | **项目独立 .venv**（8-12 新建） | ✅ `code/backend/.venv`，清华镜像装依赖 |
| **深度研究** | hello-agents（懒加载） | ⚠️ 不装；main.py 路由内 import，KB 独立运行 |
| **Docker** | Docker Desktop | ⏸ **挂起**（WSL 报 HCS/ERROR_NOT_FOUND，不阻塞主线） |
| 重排 | bge-reranker-v2-m3 | ⏸ 暂缓（触发条件：单库 chunk ≥1000） |
| Checkpointer | SqliteSaver（langgraph-checkpoint-sqlite） | ✅ P4 完成，`kb_checkpoints.db` |

---

## 二、总体架构

```
【入库链路 · 离线】
上传文档(MD/TXT/PDF/docx) → ingest.parse_document → chunk_text(800/100 重叠)
 → embeddings.embed_texts(BGE-M3 1024维) → vector_store.add_chunks(Chroma + kb_id metadata)

【问答链路 · LangGraph 实时】
用户问题(+thread_id) → rewrite(多轮改写) → retrieve(向量+BM25+RRF，kb_id 过滤)
 → generate(带 [n] 引用生成) → evaluate(LLM 忠实度 0-10，<6 重试)
 → 条件边：不达标+retries<1 → 回 generate；否则 END
 → 返回 {answer, citations, score} → 前端（答案 + 引用卡片 + 忠实度徽标）

【横切】
config.py kb_* 配置｜文档元信息聚合自 chunk metadata（list_docs）｜
多库隔离 kb_id + where 过滤（P3）｜SQLite checkpointer 按 thread_id 持久化对话（P4）
```

---

## 三、分阶段计划（含实际状态 + 量化验收）

### Phase 0：环境准备 ✅ 完成（Docker 挂起）
| # | 任务 | 状态 | 验收 |
|---|------|------|------|
| 0.1 | Docker Desktop | ⏸ 挂起 | WSL 兼容问题，不阻塞；BGE-M3 用 Ollama 直装替代 |
| 0.2 | BGE-M3 部署 | ✅ Ollama `bge-m3` | `curl /api/embed` 返回 1024 维 |
| 0.3 | `.env` 配置 | ✅ | `KB_EMBEDDING_MODE=bge_m3` 等读取成功 |
| 0.4 | 真实 BGE-M3 跑通全链路 | ✅ | 入库→检索→问答，"招投标"语义命中采购制度 |

### Phase 1：MVP ✅ 完成
| # | 任务 | 状态 | 验收 |
|---|------|------|------|
| 1.1 | kb 四模块 | ✅ | ingest/embeddings/vector_store/qa_graph |
| 1.2 | 真实 BGE-M3 接入 | ✅ | 语义检索不靠字符重叠 |
| 1.3 | 接口 ingest/ask | ✅ | curl 上传+问答返回答案+引用 |
| 1.4 | 前端对话页 | ✅ | 浏览器可上传/对话 |
| 1.5 | 集成测试 | ✅ | pytest 全绿 |

### Phase 2：质量增强（部分完成）
| # | 任务 | 状态 | 验收（量化） |
|---|------|------|-------------|
| 2.1 | 混合检索 BM25+RRF | ✅ | 10 条 QA 评测集：Top-5 命中率 ≥ 90%；"5万元"精确命中 |
| 2.2 | bge-reranker 重排 | ⏸ 暂缓 | **触发条件**：单库 chunk ≥1000 或召回噪声明显；接口已预留（HybridRetriever 输出后） |
| 2.3 | 引用溯源前端化 | ❌ 未做 | answer 内 [n] 可点击 → 展开卡片（doc_title + chunk_text 片段 + chunk_id） |
| 2.4 | evaluate LLM 打分 | ✅ | 忠实度 0-10，<6 重试；评测集上 ≥8 占比 ≥ 80% |

### Phase 3：知识库管理能力（部分完成）
| # | 任务 | 状态 | 验收 |
|---|------|------|------|
| 3.1 | 文档 CRUD | ✅ | list/delete/PUT 重新索引；删后检索不到，PUT 后立即反映新内容 |
| 3.2 | 多知识库 kb_id 隔离 | ✅ | A 库问题在 B 库检索 0 命中（实测 product 库查不到 default 采购内容） |
| 3.3 | 增量更新 | ⚠️ 当前 PUT 全量重索引 | 见缺口设计 §6.2；sha256 指纹为可选增强 |
| 3.4 | 权限 RBAC | ❌ 未做 | 见缺口设计 §6.1；当前仅 kb_id 隔离（知识库级），无用户级 |

### Phase 4：工程化 + 部署（部分完成）
| # | 任务 | 状态 | 验收 |
|---|------|------|------|
| 4.1 | pytest 全量 | ✅ | KB 11 + 深度研究 18 = 29 用例全绿（3.5s）；CI ❌ 未接 |
| 4.2 | Docker Compose | ⏸ 挂起 | 依赖 Docker 修复 |
| 4.3 | loguru 结构化日志 | ✅ | main.py 已用 loguru，错误可追踪 |
| 4.4 | checkpointer SQLite | ✅ | 同 thread_id 跨请求，第二轮引用第一轮上下文；`kb_checkpoints.db` 持久化 |

**实际工作量**：P1-P4 主体 1 人日完成（8-12，高强度）。**剩余**约 3-4 人日（引用前端化 0.5 + RBAC 1.5 + CI 0.5 + reranker 触发后 1）。

---

## 四、关键决策（v3 更新）

### 4.1 Docker：挂起（非放弃）
- **现状**：WSL 报 HCS/ERROR_NOT_FOUND，Docker Desktop 4.86 创建发行版失败。
- **影响**：BGE-M3 改用 Ollama 直装（已落地，不依赖 Docker）；Qdrant 演进延后；Compose 部署延后。
- **保留**：环境层正常（WSL2.7.11/内核 6.18/VMP），疑点在 Docker Desktop 兼容；留 `C:\Project\_docker_logs.ps1` 抓日志，后续可重试。

### 4.2 BGE-M3 已定 Ollama（原"二选一"已决策）
- 选 Ollama：`ollama pull bge-m3`，`/api/embed` 批量接口，内存占用小。
- 模型源：ModelScope（国内，Ollama 原生支持），规避 R2 CDN 断线。

### 4.3 项目独立 venv（8-12 新增，计划外）
- `code/backend/.venv`：用 managed python 3.13.12 创建，清华镜像装依赖。
- 知识库独立于 hello-agents（main.py agent 懒加载），换机器/移交自包含。
- 启动：`cd code/backend/src && ../.venv/Scripts/python.exe -m uvicorn main:app --port 8000`（启动前 unset 代理）。

### 4.4 向量库：Chroma（当前）→ Qdrant（Docker 修复后可选）
- `vector_store.py` 是薄封装，换实现接口不变。当前 Chroma 满足，不阻塞。

---

## 五、剩余任务与优先级（原"待确认"已决策）

| 优先级 | 任务 | 依赖 | 工作量 | 交付 |
|--------|------|------|--------|------|
| P0 | 引用溯源前端化（2.3） | 后端已返回 citations | 0.5 天 | KbChat.vue 引用卡片 |
| P0 | 建小评测集 + 量化验收 | 无 | 0.5 天 | 10-20 条 QA + 命中率/忠实度脚本 |
| P1 | RBAC 权限（3.4） | 用户体系需求 | 1.5 天 | 用户/角色模型 + 检索层过滤 |
| P1 | CI（4.1） | 项目上 GitHub | 0.5 天 | `.github/workflows/ci.yml` |
| P2 | bge-reranker（2.2） | chunk ≥1000 | 1 天 | reranker 接入（接口已预留） |
| P3 | Docker Compose（4.2） | WSL 修复 | 1 天 | docker-compose.yml |
| P3 | 增量更新 sha256（3.3 可选） | 无 | 0.5 天 | 指纹比对逻辑（当前 PUT 全量已够用） |

---

## 六、缺口设计补充（v3 新增）

### 6.1 RBAC 权限模型（补 3.4）
**目标**：用户级权限——不同用户可访问不同知识库，检索层强制过滤。

**数据模型**（SQLite 或 chunk metadata 旁的独立表）：
```
users(user_id PK, name)
kb_access(user_id FK, kb_id)   -- 用户可访问的 kb 列表
```

**接口**：
- `/kb/ask` 请求加 `user_id`（或 token 解析）
- 后端查 `kb_access` 得 `allowed_kb_ids`
- 检索层：`where={"kb_id": {"$in": allowed_kb_ids}}`（`_merge_where` 已支持 `$in`）
- 越权：用户无权限的 kb，检索 where 过滤掉 → 查不到（防越权在检索层，非生成后补救）

**验收**：用户 A 无 kb_B 权限 → 问 kb_B 内容返回"未检索到"。

### 6.2 增量更新（补 3.3）
**当前实现**：`PUT /kb/docs/{id}` 先删旧分块再用原 doc_id 全量重索引（保持 doc_id 稳定，引用历史不失效）。已满足"修改后立即反映新内容"。

**sha256 指纹（可选增强）**：
- 文档级 sha256 存独立表 `doc_meta(doc_id, sha256, updated_at)` 或 chunk metadata
- PUT 时先比对 sha256：相同 → 跳过重索引（省向量化开销）；不同 → 全量重索引
- **chunk 级增量**（只重算变更 chunk）：成本高（要 diff + 重新分块对齐），标注 P5 再考虑

**验收**：修改文档 → PUT → 下次问答立即反映新内容（实时，无延迟）。

### 6.3 引用溯源前端化（补 2.3）
**后端已返回**：`citations: [{index, chunk_id, text, metadata:{doc_title, kb_id, doc_id, chunk_index}}]`

**前端交互**：
- answer 内 `[n]` 渲染为可点击链接（绿色下划线）
- 点击 → 在 bubble 下方展开引用卡片：`📄 {doc_title} · kb={kb_id}` + `chunk_text[:120]...` + `#chunk_id`
- 卡片可折叠（再点收起）
- 未来增强：点击跳转文档详情页（需文档详情接口）

**验收**：问答后 answer 的 [1][2] 可点击，展开显示来源文档名与片段。

---

## 七、风险提示（v3 更新）

| 风险 | 现状/缓解 |
|------|-----------|
| Docker 安装失败 | ⏸ 已挂起；BGE-M3 用 Ollama 直装替代，不阻塞 |
| BGE-M3 模型下载 | ✅ 已用 ModelScope 解决（R2 CDN 断线） |
| 本地推理内存 | ✅ Ollama bge-m3 实测可跑（占用约 1.2GB）；保留智谱 API 降级路径（代码已支持） |
| 代理干扰 | ✅ 已知坑：`http_proxy=127.0.0.1:7897` 代理软件未开 → 服务/pip 启动前 unset |
| LangGraph checkpointer API | ✅ 已踩坑：`from_conn_string` 返回 context manager，用 `SqliteSaver(sqlite3.connect(...))` 直接实例化 |
| openai 3.0 兼容 | ✅ 已验证 `_llm_invoke` 兼容 |

---

## 八、面试弹药（v3 定稿版）

1. **为什么 LangGraph**：检索/生成/评估是天然图结构，条件边原生表达"评估不达标就重试"；对比深度研究手写编排——按场景选工具。
2. **两阶段 RAG**：召回层向量+BM25+RRF（语义与精确互补）；精排层评估过 bge-reranker，小语料暂缓，接口可插拔——知道成本收益与触发条件。
3. **防幻觉**：evaluate 节点 LLM 忠实度打分（0-10，<6 重试），失败退回规则检查；检索不到时 prompt 要求"缺少相关信息"。
4. **多知识库隔离**：单 collection + kb_id metadata + where 过滤（非 collection 命名空间）——支持跨库检索、迁移 Qdrant 接口不变、权限即 kb_id 过滤延伸。
5. **checkpointer**：SQLite 按 thread_id 持久化对话，跨请求恢复上下文，断点续跑能力。
6. **工程化**：11 个 KB pytest 用例（FakeLLM+FakeEmbedding 不联网毫秒级）、agent 懒加载让知识库独立于深度研究。
7. **踩坑=加分**：Pydantic+future annotations、ModelScope 拉模型、代理干扰、SqliteSaver context manager、相对路径——都是真实工程问题。

---

*v3 交接完毕。P1-P4 主体完成，剩余约 3-4 人日（引用前端化/RBAC/CI/reranker）。新会话结合 `.workbuddy/memory/2026-08-12.md` + `2026-08-13.md` 工作日志即可完整接手。*
