# 深度研究助手项目 — 对话交接文档

> 用途：上下文额度已满，新建对话时把本文档内容贴给新会话的 AI，即可无缝继续。
> 最后更新：2026-08-04 16:20

---

## 一、给新对话 AI 的一句话

你是一位 AI 应用工程师兼学习导师，正在带一位 **Python 零基础**的用户，用 7 天完成《Hello-Agents》第14章的"深度研究智能体"项目，目的是理解原理并写进简历。请先读下面的背景，然后从"当前进度"处继续教学。

---

## 二、项目背景（关键路径）

| 项目 | 路径 |
|------|------|
| 项目源码 | `C:\Users\venu\WorkBuddy\2026-06-05-14-10-35\hello-agents\code\chapter14\helloagents-deepresearch` |
| 第14章教程 | `C:\Users\venu\WorkBuddy\2026-06-05-14-10-35\hello-agents\docs\chapter14\第十四章 自动化深度研究智能体.md` |
| 后端入口 | `backend\src\main.py`（FastAPI，端口 8000） |
| 核心协调器 | `backend\src\agent.py`（DeepResearchAgent，546 行） |
| 前端 | `frontend\`（Vue3 + Vite，端口 5174） |
| .env 配置 | `backend\.env`（DeepSeek API + DuckDuckGo，已配置好） |
| 服务状态 | 8000 和 5174 端口当前都在运行中（如重启需按 D1 方法启动） |

**项目核心**：TODO 驱动研究范式，三个 LLM Agent（规划/总结/报告）协作，实现 规划 → 并行搜索 → 流式总结 → 报告生成 全流程，SSE 实时推送。

---

## 三、项目1目录下的文档（交付物）

| 文件 | 内容 |
|------|------|
| `C:\Users\venu\WorkBuddy\项目1\深度研究助手-7天项目计划.md` | 7 天每日计划 + 简历模板 + 面试题库 + 常见坑 |
| `C:\Users\venu\WorkBuddy\项目1\深度研究助手-项目进行计划.md` | 项目流程详解 + 知识点 + 核心代码速查 |
| `C:\Users\venu\WorkBuddy\项目1\D2笔记-main.py零基础精读.md` | main.py 零基础精读（逐行注释 + 例子 + 易错点 + 自测题） |
| `C:\Users\venu\WorkBuddy\项目1\WorkBuddy创建项目提示词.md` | 给新会话用的项目提示词模板 |

> 新对话可以先让 AI 读这两个文件：《7天项目计划》+《D2笔记-main.py零基础精读》

---

## 四、当前进度（重要！）

**时间线**：
- 8/3（第1天）：✅ 完成 —— 环境搭建、跑通项目、看懂 SSE 事件流、浏览器实操截图
- 8/4（第2天）：进行中 —— 后端架构精读
  - ✅ 已完成：main.py 全部（配置加载/数据模型/两个接口/生成器/装饰器/类型注解）
  - ✅ 已完成：models.py（TodoItem/SummaryState 数据盒子 + @dataclass + field + Optional + kw_only）
  - ✅ 已完成：config.py（SearchAPI 枚举 / Configuration 配置面板 / from_env 类方法 / **字典展开）
  - 🔄 **正在讲：agent.py 第 1 段刚讲完（__init__ 构造器：配置/LLM/三个Agent/三个Service）**
  - ⏭️ **下一步：agent.py 第 2 段 run_stream()（主流程：规划→并发执行→报告）**

**用户水平**：Python 零基础，但学习意愿强，已掌握的概念：
变量、函数/参数、类/对象、类型注解（`->`、`x: str`）、装饰器（`@app.post`）、生成器（`yield` vs `return`）、语法糖、`@dataclass`、`field(default_factory=list)`（防共享坑）、`Optional[str]`、枚举、`@classmethod`、`**字典` 展开、`lambda`、`kw_only`、`*` 关键字参数、Pydantic BaseModel/Field、SSE 格式（`data: {json}\n\n`）、CORS、`if __name__ == "__main__"`、环境变量加载顺序（load_dotenv 先于 import config）

---

## 五、教学风格要求（必须遵守）

1. **全程中文，大白话**，专业词汇加括号解释
2. 每段代码**逐行中文注释**，讲"做什么 + 为什么"
3. 用**生活类比**（蛋糕模具/快递/餐厅/工厂）帮助理解
4. 每小节结束出**理解检验题**，用户心里作答
5. 用户答错/不会时，**换更简单的例子重新讲**，不要跳过去
6. 讲解顺序：main.py → models.py → config.py → agent.py（进行中）→ services/（planner/summarizer/reporter/search）→ 前端 api.ts/App.vue
7. 每天结束前总结当天收获；所有笔记/截图存到 `C:\Users\venu\WorkBuddy\项目1\`
8. 截图统一放 `C:\Users\venu\OneDrive\图片\屏幕快照`（当前模型可能无法读图，需用户描述内容）
9. 用户在 agent.py 之前已学过的所有概念，可直接引用，不必重复讲

---

## 六、下一步动作（新对话直接从这里开始）

1. 让 AI 读取 `C:\Users\venu\WorkBuddy\项目1\` 下 4 份文档 + 本文档
2. 继续讲解 **agent.py 第 2 段：`run_stream()` 方法**（约第 151-285 行）
   - 重点：规划阶段（plan_todo_list）→ todo_list 事件 → 并发线程启动 → Queue 事件队列 → 主线程消费转发 SSE → 报告生成 → final_report/done
3. 之后按 D2 计划继续：services/ 三个文件 → 前端 → 画数据流图（D2 验收产出）

---

## 七、7 天节奏提醒

| 天 | 内容 |
|----|------|
| D1 ✅ | 环境搭建 + 跑通 + 截图 |
| D2 🔄 | 后端架构精读（main/models/config/agent + services + 前端）|
| D3 | 三个 Agent + Prompt 精读 |
| D4 | 搜索与工具系统 |
| D5 | 前端 SSE 精读 |
| D6 | 动手改造 2 个功能 |
| D7 | 简历 + 模拟面试 |

> 注意：D2 内容较多（今天已在 agent.py 中途），可适当顺延到 D3 上午，后续每天顺延半天，或按实际节奏调整。

---

## 八、遇到问题时的排查提示（项目曾修复过的坑）

| 问题 | 原因 | 解决 |
|------|------|------|
| 子任务 failed、无来源 | ddgs 库 backend 参数问题 | 已在 search_tool.py 修复（去掉 backend 参数+重试）|
| LLM 流式中断 | DeepSeek 断连无重试 | 已在 llm.py 修复（重试+非流式 fallback）|
| 代理 127.0.0.1:7897 拒绝 | 代理软件未启动 | 检查代理或设 NO_PROXY |
| 端口占用 | 旧进程未退出 | taskkill /PID <pid> /F |
